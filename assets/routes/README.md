# Route Files

Add route tracks as GPX files in this folder, then add one row to `../../routes.csv`.

The page reads `routes.csv` at runtime, so adding a route should only require:

1. Uploading a GPX file here.
2. Adding a matching metadata row to `routes.csv`.

Use pipe separators in the CSV, matching the FAQ setup.
