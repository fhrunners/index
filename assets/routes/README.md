# Route Files

Add route tracks as GPX files in this folder, then add one entry to `../../routes.json`.

The page reads `routes.csv` at runtime, so adding a route should only require:

1. Uploading a GPX file here.
2. Adding a matching metadata object to `routes.json`.
